# Copy this file to .env.local (never commit .env.local) and set:
#   VITE_USE_MOCK=false
# once your teammate's FastAPI backend is running.
# If it's on a different host/port, also set VITE_API_BASE.

VITE_USE_MOCK=true
VITE_API_BASE=http://localhost:8000/api
